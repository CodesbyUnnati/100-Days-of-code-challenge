#include<iostream>
#include<algorithm>
using namespace std;
void show(int a[])
{
  for(int i=0;i<10;i++)
  cout<<a[i]<<" ";
}
int main()
{
  int a[10]={ 10,56,7,4,51,89,0,33,65,42};
  cout<<"Before Sorting: ";
  show(a);
  sort(a,a+10);
  cout<<"\n After sorting: ";
  show(a);
  return 0;
}
