#include<stdio.h>
int main()
{
  int i,j,rows,number=1;
  printf("Enter number of Rows: ");
  scanf("%d",&rows);
  for(i=0;i<rows;i++)
  {
    for(j=0;j<=i;j++)
    {
      printf("%d",number);
      ++number;
    }
    printf("\n");
  }
  return 0;
}
